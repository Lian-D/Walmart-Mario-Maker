class Button{
    // fields
    name:String;
    traits:Array<Statement>;

    // constructor

    constructor(name:String, traits:Array<Statement>){
        this.name = name;
        this.traits = traits;
    }

    // functions
}